# 成员2：系统与权限单元测试结果

## 执行信息

| 项目 | 结果 |
| --- | --- |
| 执行日期 | 2026-07-21 |
| 基线 commit SHA | `dc05ce8c81bfdba0984b26bd22c55cef7d52d5d2` |
| 测试工程 | `源码/iMES.Net/iMES.System.Tests` |
| 测试框架 | xUnit + Microsoft.NET.Test.Sdk + Coverlet collector |
| 运行方式 | Docker `.NET Core 3.1 SDK` |
| 外部依赖 | 不连接 SQL Server、Redis，不调用 HTTP API |

## 执行命令

在仓库根目录执行：

```bash
docker build -t imes-system-tests:local -f 源码/iMES.Net/iMES.System.Tests/Dockerfile 源码/iMES.Net
```

## 结果汇总

| 项目 | 数量 |
| --- | --- |
| 总用例数 | 16 |
| 通过 | 16 |
| 失败 | 0 |
| 阻塞 | 0 |
| 待产品确认 | 0 |

## 覆盖范围

| 模块 | 覆盖点 | 路径类型 | 结果 |
| --- | --- | --- | --- |
| `Sys_User` 输入契约 | 新增用户移除主键和未知字段 | 成功路径 | 通过 |
| `Sys_User` 输入契约 | 用户名缺失被拒绝 | 失败路径 | 通过 |
| `Sys_User` 输入契约 | 真实姓名超过 40 字符被拒绝 | 边界路径 | 通过 |
| `Sys_Dictionary` 输入契约 | 新增字典移除主键和未知字段 | 成功路径 | 通过 |
| `Sys_Dictionary` 输入契约 | 字典编号缺失被拒绝 | 失败路径 | 通过 |
| `Sys_DictionaryList` 输入契约 | 字典明细值超过 100 字符被拒绝 | 边界路径 | 通过 |
| `Sys_Dictionary` 主从契约 | 主表声明 `Sys_DictionaryList` 为字典明细 | 成功路径 | 通过 |
| `UserContext` | 仅角色 ID 为 1 时识别为超级管理员 | 成功/失败路径 | 通过 |
| `ActionPermissionOptions` | `Add/Delete/Update/Search/Export` 按钮权限值稳定 | 契约路径 | 通过 |
| `LoggerType` | `Add/Edit/Del/Export` 审计日志类型名称稳定 | 契约路径 | 通过 |
| `ResponseType` | 越权和角色越权使用独立失败码 | 失败路径 | 通过 |
| `ApiActionPermissionAttribute` | 显式表名授权生成 `Search` 权限要求 | 成功路径 | 通过 |
| `ApiActionPermissionAttribute` | 代码生成控制器授权生成 `SysController` 权限要求 | 成功路径 | 通过 |
| `ApiActionPermissionAttribute` | 角色限制授权生成超级管理员角色要求 | 成功路径 | 通过 |
| `Permissions` | 仅有 `Search` 不隐含 `Add/Delete` | 失败路径 | 通过 |
| `RoleAuthor` | 角色授权载体保留菜单和动作结构 | 成功路径 | 通过 |

## 失败用例和证据

本次系统与权限单元测试最终结果为 **16 通过，0 失败**，无失败用例。

Docker 输出关键结果：

```text
Test Run Successful.
Total tests: 16
     Passed: 16
Total time: 1.0541 Seconds
```

## 集成回归参考

`tests/测试文件/run-authorization-regression.mjs` 已覆盖 AUTH-01 的真实接口行为：目录、菜单、按钮权限、角色授权缓存刷新、越权拒绝和 `Add/Edit/Export/Del` 审计日志。该脚本依赖 SQL Server、Redis 和 API 容器，不计入本单元测试通过数。

## 阶段一结论

系统与权限领域已建立 `iMES.System.Tests` 单元测试工程和可重复执行 Docker 命令。本轮只提交测试工程、测试源文件和结果文档，不修改业务规则。
