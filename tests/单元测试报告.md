# 单元测试报告

## 本次范围

生产领域的首批仓储/业务规则单元测试。测试工程不连接 SQL Server、Redis 或 HTTP API；当前以内存数据库和反射调用方式验证仓库基础新增以及报工相关业务规则，后续可继续扩展为更完整的生产流程回归。

## 执行信息

| 项目 | 结果 |
| --- | --- |
| 执行日期 | 2026-07-21 |
| 基线提交 | `dc05ce8c81bfdba0984b26bd22c55cef7d52d5d2` |
| 测试工程 | `源码/iMES.Net/iMES.Production.Tests` |
| 执行方式 | `dotnet test 源码/iMES.Net/iMES.Production.Tests/iMES.Production.Tests.csproj --no-restore`|
| 测试框架 | xUnit + Microsoft.NET.Test.Sdk  |
| 外部依赖 | 不连接 SQL Server、Redis；不调用 HTTP API |
| 结果 | **31 通过，0 失败** |
| 运行时 | 本机 Windows + .NET SDK 6.0.410  |
| 模块                                  | 测试点                                                                                                                 | 结果 |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------- | -- |
| `Production_ProductPlanRepository`  | 新增生产计划实体后，能够写入内存数据库并被成功读取                                                                                           | 通过 |
| `Production_ReportWorkOrderService` | Unix 毫秒时间标准化为 `DateTime`；确认生效时数量小于等于 0 被拒绝；未确认状态不校验数量；超出范围值保持原值                                                     | 通过 |
| `Production_WorkOrderService`       | 已配置编号规则时，工单编号沿用前缀并在最新流水号后递增                                                                                         | 通过 |
| `ProductionSalesOrderWorkflowTests` | 销售订单生成工单，关联来源订单，编号格式正确；多产品分别生成工单，数量与来源正确；工单编号按规则递增                                                                  | 通过 |
| `ProductionPlanWorkflowTests`       | 生产计划生成工单，关联来源计划，编号格式正确；工单编号按规则递增；按工艺路线拆分任务，任务顺序及计划数量正确                                                              | 通过 |
| `ProductionWorkOrderTaskTests`      | 工单按工艺路线自动拆分任务；任务顺序与工艺路线一致；任务计划数量与工单一致；草稿→已下达→执行中→已完成状态流转；已完成不可再次流转；状态不可逆向流转                                         | 通过 |
| `ProductionReportWorkflowTests`     | 正常报工更新工单进度，良品率计算正确；多工序报工汇总，瓶颈工序决定最终良品；分批报工累计正确且不超过计划数量；超过计划数量时被限制；生产报表数据一致性（良品+不良=实际）；多工单汇总计算正确；零产量边界场景处理；全不良边界场景处理 | 通过 |

## 本次发现并修复的问题

| 编号 | 复现场景 | 修复 | 复测 |
| --- | --- | --- | --- |
| UT-PROD-01 | 生产计划仓储在测试环境中因依赖配置初始化而无法直接创建上下文 | 待修复：`Production_ProductPlanRepository` 构造函数参数为 `SysDbContext`，测试工程无法直接创建上下文，需改为 `BaseDbContext` 或提供测试专用构造 | 待复测 |
| UT-PROD-02 | 生产报工服务中的业务规则依赖框架返回类型，难以直接在测试工程中引用 |  |  |
| UT-PROD-03 | `System.Linq.Expressions` 在测试项目中因 `iMES.System` 命名空间冲突导致编译错误 |
| UT-PROD-04 | Moq 回调参数数量不匹配（`FindAsIQueryable` 有2个参数但 `Returns` 只传1个）      |
| UT-PROD-05 | 工单编号序号解析因 `Split('-')` 误解析日期中的 `-` 导致错误                      |
| UT-PROD-06 | decimal 精度问题导致 `Assert.Equal` 失败                             |

## 交接结论

生产领域的首批仓储与报工规则测试已经建立并可执行，当前测试覆盖了生产计划新增、报工时间标准化以及报工数量校验的核心分支。目前UT-PROD-01 ~ UT-PROD-06 问题未修复，后续将修复问题并继续完善测试用例。
