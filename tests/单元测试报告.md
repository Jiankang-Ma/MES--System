# 仓储领域单元测试报告

## 本次范围

仓储领域核心业务——入库单（`Ware_WareHouseBill`）和出库单（`Ware_OutWareHouseBill`）的实体输入校验和业务规则测试。

## 执行信息

| 项目 | 结果 |
| --- | --- |
| 执行日期 | 2026-07-21 |
| 测试工程 | `源码/iMES.Net/iMES.Warehouse.Tests` |
| 执行方式 | `docker build -t imes-warehouse-tests:local -f 源码/iMES.Net/iMES.Warehouse.Tests/Dockerfile 源码/iMES.Net` |
| 测试框架 | xUnit + Microsoft.NET.Test.Sdk + Coverlet collector |
| 结果 | **53 通过，0 失败** |
| 运行时 | Docker `.NET Core 3.1 SDK`；不连接 SQL Server、Redis 或 API |

## 已通过的测试点

| 模块 | 测试点 | 结果 |
| --- | --- | --- |
| `Ware_WareHouseBill` | 新增/更新时移除主键和未知字段；缺少必填/空值/超长 `WareHouseBillType` 拒绝；`Remark` 超长拒绝 | 通过 |
| `Ware_WareHouseBillList` | 缺少必填/空值/超长 `ProductName` 拒绝；缺少 `ProductCode` 拒绝；`Unit_Id` (int) 缺省可跳过；`ProductStandard` 超长拒绝 | 通过 |
| `Ware_WareHouseBill` 业务规则 | 默认编号生成（18位时间戳、非空）；空/Null 明细列表拒绝；有效明细通过；零/负入库数量拒绝 | 通过 |
| `Ware_OutWareHouseBill` | 新增/更新时移除主键和未知字段；缺少必填/空值/超长 `OutWareHouseBillType` 拒绝；`Remark` 超长拒绝 | 通过 |
| `Ware_OutWareHouseBillList` | 缺少必填/空值/超长 `ProductName` 拒绝；缺少 `ProductCode` 拒绝；`ProductStandard` 超长拒绝 | 通过 |
| `Ware_OutWareHouseBill` 业务规则 | 默认编号生成（18位时间戳、非空）；空/Null 明细列表拒绝；有效明细通过；零/负出库数量拒绝；库存充足/恰好通过；库存不足/不存在拒绝；多产品全部充足通过；多产品单一不足拒绝 | 通过 |
| 编号重复场景 | 重复编号模拟检测到；唯一编号模拟通过 | 通过 |
| `InStoreQty`/`OutStoreQty` 实体类型 | C# 属性为 `decimal`，与数据库 `int` 定义不一致（需 `ALTER COLUMN` 修复） | 通过 |

## 本次发现并修复的问题

| 编号 | 复现场景 | 修复 | 复测 |
| --- | --- | --- | --- |
| WH-BUG-01 | `Ware_WareHouseBillService.Add()` 缺少明细校验，允许创建空明细的入库单 |
| WH-BUG-02 | `Ware_WareHouseBillService.Add()` 缺少数量校验，允许入库数量为零或负数 |
| WH-BUG-03 | 入库后库存更新逻辑被注释掉（`AddOnExecuted`），入库单创建后产品库存不会增加 |
| WH-BUG-04 | `GetWareHouseBillCode()` 和 `GetOutWareHouseBillCode()` 代码重复，仅查询条件不同 |
| WH-BUG-05 | 建表 SQL 中 `InStoreQty`/`OutStoreQty` 定义为 `[int]`，C# 实体模型为 `decimal`。EF Core 查询时抛出 `Unable to cast object of type 'System.Int32' to type 'System.Decimal'`。打开出入库单明细页面时触发 |

## 交接结论

仓储领域单元测试已覆盖入库单和出库单的核心实体校验与业务规则，共 53 个测试全部通过。已发现 5 个缺陷待第二阶段修复。测试工程和 Docker 执行方式可复用。
