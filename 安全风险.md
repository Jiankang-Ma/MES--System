
## SQL INJECTION 风险：
`原Production_ReportWorkOrderController第87行的getReportDetailInfo函数`:执行SQL语句时，直接用的字符串拼接写法，有SQL注入攻击的风险。攻击者若已登录并能访问该接口，可能通过构造 workOrderId 改变原本的 WHERE 条件，造成越权读取其他工单的报工数据的风险；在数据库连接权限过高时，理论上可能进一步读取、修改或删除其他表数据。还有可以通过异常或延时行为探测数据库结构。

## 已修复：报工明细 SQL 注入（2026-07-15）

### 影响接口

`GET /api/Production_ReportWorkOrder/getReportDetailInfo?workOrderId=...`

代码位置：[Production_ReportWorkOrderController.cs](/Users/yuanzhuoshen/Desktop/MES系统源码Docker部署/源码/iMES.Net/iMES.WebApi/Controllers/Production/Partial/Production_ReportWorkOrderController.cs:85)。

### 修改内容

修复前将 `workOrderId` 直接拼接到 SQL 字符串。修复后改为 Dapper 参数绑定：

```csharp
const string woSql = @"... WHERE rwo.WorkOrder_Id = @workOrderId";
var list = DBServerProvider.SqlDapper.QueryList<Production_ReportWorkOrder>(
    woSql,
    new { workOrderId });
```

`workOrderId` 会作为 SQL 参数传递，而不是 SQL 语法的一部分；特殊字符只能作为查询值处理，无法改变 `WHERE` 条件或附加 SQL 语句。查询字段、连接关系和响应格式均未改变。

### 验证方式

重新构建并启动 API 后，应验证：

1. 使用正常工单 ID 请求仍返回原有报工明细。
2. 使用不匹配或含特殊字符的值不会导致 SQL 异常、扩大查询范围或执行其他语句。

## 风险

以下为静态扫描发现的候选项，尚未在本轮修改，需按风险优先级继续处理：

| 优先级 | 位置 | 风险与建议 |
| --- | --- | --- |
| 高 | `Production_WorkOrderService.GetPageData` | 工单编号被拼接到传入 `SerializeJSON` 的动态 SQL 中。工单编号可由业务数据输入，应改造存储过程/调用方式，避免把数据值拼入可执行 SQL。 |
| 高 | `Production_AssembleWorkOrderService.GetPageData` | 与生产工单页相同的 `WorkOrderCode` 动态 SQL 拼接模式，应一并参数化或改为专用存储过程参数。 |
| 高 | `Base_PrintTemplateController` | `saveOrUpdateData`、`getBillData`、`getDetailData` 等接口允许匿名访问。任意访问者可修改非内置打印模板，或按单据 ID 读取销售订单、计划、装配/出入库单及明细。应移除 `AllowAnonymous`，并按单据所属权限做授权校验。 |
| 高 | `Sys_LogController.Test` | `GET /api/Sys_Log/test` 允许匿名访问且每次写入 10 条日志。可被滥用造成日志污染、数据库增长和资源耗尽；应删除该测试端点，或至少移除匿名访问并限制为开发环境。 |
| 高 | `appsettings.json` | 源码中包含 SQL Server/Redis 明文凭据，以及固定 JWT、用户加密密钥。若源码或构建产物泄露，攻击者可伪造 Token 或访问数据库。应立即轮换现有密钥和密码，将生产值移至部署密钥管理或环境变量，并提交不含真实值的配置模板。 |
| 中 | `Startup.ConfigureServices` | 虽读取 `CorsUrls`，实际却配置 `AllowAnyOrigin()`，所有来源都可跨域调用 API。应改为 `WithOrigins` 且只允许明确的 HTTPS 前端域名。 |
| 中 | `docker-compose.yml` | API 数据库连接配置使用 `sa`。生产环境应创建最小权限应用账号，禁止应用使用系统管理员账号。 |
| 中 | `Login.vue` | 页面直接公开 `admin / 123456` 演示凭据。部署前应移除提示并修改默认密码。 |
| 中 | `Sys_DictionaryService` | 字典配置中的 `DbSql` 会作为动态 SQL 片段执行。应限制能编辑该配置的角色，并对可执行语句采用白名单或只读视图。 |
| 中 | `Startup.Configure` | `/Upload` 目录以匿名静态文件公开，且 `ServeUnknownFileTypes = true`。需要审查上传接口；建议限制上传类型、阻止 HTML/脚本等可执行内容、设置 `X-Content-Type-Options: nosniff`，并对私有文件做授权下载。 |

### 扫描边界

本轮为静态代码审计：检查了应用自有 C#、Docker 配置和前端源码，排除了 `bin`、`obj`、`wwwroot` 下的第三方库。未对运行中的服务发送注入、枚举、暴力尝试或其他攻击载荷。上述待处理项需要在预发布环境中完成授权回归和业务验证后再上线。
