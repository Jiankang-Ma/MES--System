
## SQL INJECTION 风险：
`原Production_ReportWorkOrderController第87行的getReportDetailInfo函数`:执行SQL语句时，直接用的字符串拼接写法，有SQL注入攻击的风险。攻击者若已登录并能访问该接口，可能通过构造 workOrderId 改变原本的 WHERE 条件，造成越权读取其他工单的报工数据的风险；在数据库连接权限过高时，理论上可能进一步读取、修改或删除其他表数据。还有可以通过异常或延时行为探测数据库结构。

## 已修复：报工明细 SQL 注入（2026-07-15）

### 影响接口

`GET /api/Production_ReportWorkOrder/getReportDetailInfo?workOrderId=...`

代码位置：[Production_ReportWorkOrderController.cs](/Users/yuanzhuoshen/Desktop/MES系统源码Docker部署/源码/iMES.Net/iMES.WebApi/Controllers/Production/Partial/Production_ReportWorkOrderController.cs:85)。

### 修改内容

修复前将 `workOrderId` 直接拼接到 SQL 字符串。修复后改为 Dapper 参数绑定：

```csharp
const string woSql = @"... WHERE rwo.WorkOrder_Id = @workOrderId";
var list = DBServerProvider.SqlDapper.QueryList<Production_ReportWorkOrder>(
    woSql,
    new { workOrderId });
```

`workOrderId` 会作为 SQL 参数传递，而不是 SQL 语法的一部分；特殊字符只能作为查询值处理，无法改变 `WHERE` 条件或附加 SQL 语句。查询字段、连接关系和响应格式均未改变。

### 验证方式

重新构建并启动 API 后，应验证：

1. 使用正常工单 ID 请求仍返回原有报工明细。
2. 使用不匹配或含特殊字符的值不会导致 SQL 异常、扩大查询范围或执行其他语句。

## 风险

以下为静态扫描发现的候选项，经 2026-07-22 复核后按“确认、部分确认、潜在”重新标记；可低影响修改的项目已完成代码修复，状态为 Remediated，待动态复测后再标记 Closed。

| 优先级 | 位置 | 确认状态 | 本轮处理 | 风险与建议 |
| --- | --- | --- | --- | --- |
| 高 | `Production_WorkOrderService.GetPageData` | 未复现 | 未修改 | 当前代码已使用 `@workOrderCode`、`@processLineId` 参数绑定调用 `Func_GetProcessLineAndProgressByID`，未再发现传入 `SerializeJSON` 的拼接 SQL。 |
| 高 | `Production_AssembleWorkOrderService.GetDetailPage` | 部分确认 | Remediated | 实际拼接点位于 `GetDetailPage`，已改为 Dapper 参数绑定并保持 `ProductionSchedule` JSON 字符串格式不变。 |
| 高 | `Base_PrintTemplateController.saveOrUpdateData` | 确认 | Remediated | 已移除 `AllowAnonymous`，并增加 `ApiActionPermission("Base_PrintTemplate", ActionPermissionOptions.Update)`；匿名用户不再可保存或修改打印模板。 |
| 高 | `Base_PrintTemplateController` 读取类接口 | 部分确认 | 暂缓 | `getResourceByCatId`、`getDataById`、`getDataByIdPrint`、`getBillData`、`getDetailData` 仍需结合打印设计器、打印预览和现场打印链路确认后分接口授权，不在本轮一次性移除匿名。 |
| 中 | `Sys_LogController.Test` | 确认 | Remediated | 已移除 `AllowAnonymous`，增加 `ApiActionPermission(ActionRolePermission.SuperAdmin)`，并通过 `ApiExplorerSettings(IgnoreApi = true)` 从 Swagger 隐藏。 |
| 高 | `appsettings.json` | 确认 | 暂缓 | 明文连接串、Redis、JWT 和用户密钥不能用占位值替换来冒充修复；需执行真实密钥轮换、最小权限账号替换和部署密钥管理。 |
| 中 | `Startup.ConfigureServices` | 部分确认 | 暂缓 | `AllowAnyOrigin()` 需按开发/生产环境拆分白名单；直接收紧可能导致当前前端跨域和联调失败。 |
| 中 | `docker-compose.yml` | 确认 | 暂缓 | API 数据库连接配置使用 `sa`；应创建最小权限应用账号并回归数据库权限，不在本轮直接替换口令。 |
| 中 | `Login.vue` | 确认 | 暂缓 | 页面展示演示凭据；部署前应移除提示并修改默认密码，需要同步测试账号说明。 |
| 中 | `Sys_DictionaryService` | 潜在 | 暂缓 | 字典配置中的 `DbSql` 会作为动态 SQL 片段执行；需确认低权限用户是否可编辑该配置，再改白名单模板或只读视图。 |
| 中 | `Startup.Configure` | 潜在 | 暂缓 | `/Upload` 目录匿名静态公开且 `ServeUnknownFileTypes = true`；需结合上传链路、历史文件访问和下载鉴权专项整改。 |

本轮完整修复记录见 `MES系统安全测试报告/Summary/06_静态扫描候选项低影响修复记录.txt`。

### 扫描边界

本轮为静态代码审计：检查了应用自有 C#、Docker 配置和前端源码，排除了 `bin`、`obj`、`wwwroot` 下的第三方库。未对运行中的服务发送注入、枚举、暴力尝试或其他攻击载荷。上述待处理项需要在预发布环境中完成授权回归和业务验证后再上线。
